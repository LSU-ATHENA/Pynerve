// Auto-generated by CMake - DO NOT EDIT MANUALLY
//
// This file defines feature availability macros for conditional compilation.
// Use these macros to guard code that requires optional dependencies.

#pragma once
#include <cstddef>
#include <cstdint>

#include "nerve/core_types.hpp"

namespace nerve {

// Memory Unit Constants
constexpr std::size_t BYTES_PER_KB = 1024ULL;
constexpr std::size_t BYTES_PER_MB = 1024ULL * 1024ULL;
constexpr std::size_t BYTES_PER_GB = 1024ULL * 1024ULL * 1024ULL;

// Version Constants
constexpr int kVersionMajor = @PROJECT_VERSION_MAJOR@;
constexpr int kVersionMinor = @PROJECT_VERSION_MINOR@;
constexpr int kVersionPatch = @PROJECT_VERSION_PATCH@;

// Memory and Dimension Constants
constexpr std::size_t kDefaultMemoryPoolSize = BYTES_PER_GB;  // 1GB
constexpr std::size_t kMaxSimplexDimension = 10;
constexpr int kDefaultBlockSize = 256;
constexpr int kWarpSize = 32;
constexpr double kDefaultTolerance = 1e-12;
constexpr float kFloatTolerance = 1e-6f;
constexpr int kDefaultCudaDevice = 0;
constexpr int kMaxExactDimension = 5;
constexpr std::size_t kDefaultStreamingBufferSize = 10000;
constexpr std::size_t kDefaultCheckpointInterval = 1000;

// Feature Availability Constants (compile-time)
#cmakedefine01 HAS_CUDA
#cmakedefine01 HAS_PYTORCH
#cmakedefine01 HAS_MPI
#cmakedefine01 HAS_EIGEN
#cmakedefine01 HAS_NUMA

// Convenience boolean constants for C++ use
constexpr bool hasCuda = static_cast<bool>(HAS_CUDA);
constexpr bool hasPyTorch = static_cast<bool>(HAS_PYTORCH);
constexpr bool hasMpi = static_cast<bool>(HAS_MPI);
constexpr bool hasEigen = static_cast<bool>(HAS_EIGEN);
constexpr bool hasNuma = static_cast<bool>(HAS_NUMA);

}  // namespace nerve

// C Preprocessor macros for #ifdef guards
#if HAS_CUDA
  #ifndef NERVE_HAS_CUDA
    #define NERVE_HAS_CUDA 1
  #endif
  #ifndef CUDA_AVAILABLE
    #define CUDA_AVAILABLE 1
  #endif
#endif

#if HAS_PYTORCH
  #ifndef NERVE_HAS_TORCH
    #define NERVE_HAS_TORCH 1
  #endif
  #ifndef NERVE_HAS_PYTORCH
    #define NERVE_HAS_PYTORCH 1
  #endif
  #ifndef PYTORCH_AVAILABLE
    #define PYTORCH_AVAILABLE 1
  #endif
#endif

#if HAS_MPI
  #ifndef NERVE_HAS_MPI
    #define NERVE_HAS_MPI 1
  #endif
  #ifndef MPI_AVAILABLE
    #define MPI_AVAILABLE 1
  #endif
#endif

#if HAS_EIGEN
  #ifndef NERVE_HAS_EIGEN
    #define NERVE_HAS_EIGEN 1
  #endif
  #ifndef EIGEN_AVAILABLE
    #define EIGEN_AVAILABLE 1
  #endif
#endif

#if HAS_NUMA
  #ifndef NERVE_HAS_NUMA
    #define NERVE_HAS_NUMA 1
  #endif
  #ifndef NUMA_AVAILABLE
    #define NUMA_AVAILABLE 1
  #endif
#endif
