# Nerve - Persistent Homology library
# Install with development extras:
#   pip install -r requirements.txt
#
# For editable source install:
#   pip install -e "python[dev]"
#
# The authoritative dependency list lives in python/pyproject.toml.
# This file is kept in sync for users who prefer pip install -r.
# See README.md and python/pyproject.toml for details.

numpy>=1.21.0
scipy>=1.7.0
